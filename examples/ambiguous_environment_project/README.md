# Ambiguous Environment Fixture

This fixture contains intentionally mixed development endpoints for Phase 2 AI
context assessment. The documentation preview runs at http://localhost:5173.

The deterministic scanner should retain every finding. A future local analyzer
can explain which development references are intentional without changing the
Phase 1 release gate by default.
